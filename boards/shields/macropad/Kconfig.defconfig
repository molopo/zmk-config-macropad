if SHIELD_MACROPAD_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Macropad"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_MACROPAD_LEFT || SHIELD_MACROPAD_RIGHT

config ZMK_SPLIT
    default y

endif